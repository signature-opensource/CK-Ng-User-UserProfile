Adds the preferred workspace to the CK.Ng.UserProfile `/profile` page.

Displays it, adds its form control, pushes the SetPreferredWorkspaceId command into the profile update
batch, and exposes the workspaces the user belongs to on the user service.
