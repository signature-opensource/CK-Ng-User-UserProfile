Adds the first name and last name to the CK.Ng.UserProfile `/profile` page.

Two read-only properties, two form controls, and the SetUserNames command pushed into the profile
update batch - only for the fields that actually changed. Also computes the avatar initials when the
application displays a user info box.

Transformers and translations only: no component, no service of its own.
